# Copiar este archivo como .env y completar con valores reales.
# NUNCA subir el .env real a git (agregarlo a .gitignore).

REPLICATION_PASSWORD=
APP_PASSWORD=
ADMIN_PASSWORD=
PGADMIN_PASSWORD=
GRAFANA_PASSWORD=
